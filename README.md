# itb-plugin-hcert-decoder

QR / HC1 / CWT / SHLink decoding as GITB REST services. Source moves here from
WHO-ITB/gdhcn-helper (Python + zbar); image published to GHCR so adopters never `build:`.
Provides capability `hcert-decode`. Test QRs (1234.png, NO_PIN.png, icvp.png, signed.jpg)
move into suite/data/.
